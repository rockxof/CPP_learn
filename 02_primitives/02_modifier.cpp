#include <iostream>
using namespace std;

int main(){

    unsigned rock = -500;
    // this will throw an error because you can't store negative values there;

    long long largeTeaStorage = 1000000000000;
    // we can declare long long too for the biggest numbers store

    short shortNumber = 32767;

    cout << shortNumber <<endl;
    return 0;
}